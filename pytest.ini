[pytest]
markers =
    live: exercises the real npm registry over the network (requires network)
    docker: requires a working Docker daemon (real isolated npm install)
addopts = -ra
testpaths = tests
